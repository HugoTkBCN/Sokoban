/*
** EPITECH PROJECT, 2018
** get line size nbr
** File description:
** sokoban
*/

#include "../include/my.h"

int get_nbr_lines(char **map)
{
    int i;

    for (i = 0; map[i]; i++) {
    }
    return (i);
}

int get_size_line(char *map)
{
    int i;

    for (i = 0; map[i]; i++) {
    }
    return (i);
}
